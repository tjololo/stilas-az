---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: productapiversions.apim.azure.stilas.418.cloud
spec:
  group: apim.azure.stilas.418.cloud
  names:
    kind: ProductApiVersion
    listKind: ProductApiVersionList
    plural: productapiversions
    singular: productapiversion
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ProductApiVersion is the Schema for the productapiversions API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ProductApiVersionSpec defines the desired state of ProductApiVersion
            properties:
              foo:
                description: VName - Version identifier for the API Version.
                type: string
              versionDescription:
                description: VersionDescription - Description of the API Version.
                type: string
              versioningScheme:
                default: Segment
                description: VersioningScheme - An value that determines where the
                  API Version identifer will be located in a HTTP request.
                enum:
                - Header
                - Query
                - Segment
                type: string
            required:
            - foo
            type: object
          status:
            description: ProductApiVersionStatus defines the observed state of ProductApiVersion
            properties:
              apiVersionSetID:
                description: |-
                  INSERT ADDITIONAL STATUS FIELD - define observed state of cluster
                  Important: Run "make" to regenerate code after modifying this file
                type: string
              pollerToken:
                description: ResumeToken - The token used to track long-running operations.
                type: string
              provisioningState:
                description: 'ProvisioningState - The provisioning state of the API.
                  Possible values are: Creating, Succeeded, Failed, Updating, Deleting,
                  and Deleted.'
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
